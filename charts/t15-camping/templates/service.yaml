apiVersion: v1
kind: Service
metadata:
  name: {{ include "t15-camping.fullname" . }}
  labels:
    {{- include "t15-camping.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "t15-camping.selectorLabels" . | nindent 4 }}
