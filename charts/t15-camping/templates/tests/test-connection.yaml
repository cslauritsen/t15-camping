apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "t15-camping.fullname" . }}-test-connection"
  labels:
    {{- include "t15-camping.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "t15-camping.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
