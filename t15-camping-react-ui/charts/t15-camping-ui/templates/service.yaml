apiVersion: v1
kind: Service
metadata:
  name: {{ include "t15-camping-ui.fullname" . }}
  labels:
    {{- include "t15-camping-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "t15-camping-ui.selectorLabels" . | nindent 4 }}
